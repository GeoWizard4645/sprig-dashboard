# Local secrets — never commit Wi-Fi credentials or API keys
config.py
secrets.py
# Coinbase / API credentials — NEVER commit these
cdp_api_key.json
*_api_key.json
*.pem
*.key
*private*key*

# Python / MicroPython artifacts
__pycache__/
*.pyc
*.mpy

# Editor / OS
.DS_Store
.vscode/
.idea/
